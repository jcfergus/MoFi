import { MefiAuthService } from "../../../services/common/MefiAuthService";
import { MefiPostsService } from "../../../services/mefi/MefiPostsService";

// const sessionJson = require("../../session.json");

declare var describe: any;
declare var it: any;
declare var expect: any;

describe("Fetch posts", () => {
    it("Will retrieve posts from the front page.", () => {
        /* const authService = new AuthService(sessionJson);
        const postFetcher = new MefiPosts(authService);

        return postFetcher.fetchPosts(1)
            .then((posts) => {
                console.log("Got posts.");
                console.log(util.inspect(posts));
            })
            .catch((err) => {
            });
            */
    });
});
